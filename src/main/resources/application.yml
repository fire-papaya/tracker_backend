server:
  port: 9000

spring:
  application:
    name: tracker_backend
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    open-in-view: false
  datasource:
    url: jdbc:mysql://localhost:3310/uzwarcom
    username: root
    password:
    driver-class-name: org.mariadb.jdbc.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
